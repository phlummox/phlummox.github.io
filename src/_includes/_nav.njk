  <nav class="navbar-fixed-top">
      <div class="nav-menu">
          <div class="navnew-header nav">
              <a class="navbar-brand goto-top visible-xs" aria-label="navigate to top of page" href="#">#</a>
              <button class="toggle visible-xs navbar-toggle" id="navbar-toggle-btn"
                      aria-label="toggle navigation menu">
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
              </button>
          </div>
          <div class="expandable-content-xs navbar-body nav" data-expanded="false">
                <!-- navbar links -->
                <ul class="navbar-nav nav nav-links-lefty">
                  {%- for menu_item in siteinfo.main_menu %}
                  <li{% if page.url == menu_item.url %} aria-current="page"{% endif %}><a href="{{ menu_item.url | url }}">{{ menu_item.name }}</a></li>
                  {%- endfor %}
                </ul>
                <!-- navbar icons -->
                <ul class="nav navbar-nav">
                  {%- for menu_item in siteinfo.icon_menu %}
                  <li class="navbar-icon"><a href="{{ menu_item.url | url }}" rel="external"  aria-label="{{menu_item.description}}"><i class="fa fa-{{ menu_item.name }}"></i></a></li>
                  {%- endfor %}
                </ul>
          </div><!-- end menu items -->
      </div>
  </nav>
{#- vim: syntax=html :
#}
